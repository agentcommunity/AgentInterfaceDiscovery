{
  "name": "aid-examples-host",
  "private": true,
  "description": "A Vercel project to host all AID example manifests.",
  "scripts": {
    "build-examples": "pnpm --filter @agentcommunity/aid-core run build:examples",
    "build": "pnpm run build-examples"
  }
} 