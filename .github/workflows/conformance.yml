name: Conformance Check

on:
  push:
    branches: ["main"]
  pull_request:
    branches: ["main"]

jobs:
  conformance:
    runs-on: ubuntu-latest
    steps:
      - name: 1️⃣ Checkout repository
        uses: actions/checkout@v4

      - name: 2️⃣ Setup pnpm and Node.js
        # This action correctly sets up Node and then pnpm
        uses: pnpm/action-setup@v3
        with:
          version: 8
      - uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: 'pnpm'

      - name: 3️⃣ Install dependencies & Build
        run: pnpm install --frozen-lockfile

      - name: 4️⃣ Lint
        run: pnpm run lint

      - name: 5️⃣ Schema drift guard
        run: pnpm run schema:check

      - name: 6️⃣ Unit tests with coverage
        run: pnpm --filter @agentcommunity/aid-conformance test

      - name: 7️⃣ Validate committed examples
        run: pnpm --filter @agentcommunity/aid-conformance run test:conformance ./packages/examples/public || echo "conformance script not yet implemented"

      - name: 8️⃣ Bundle size check
        run: |
          gzip -c packages/aid-core/dist/browser/index.mjs | wc -c > size.txt
          LIMIT=16000
          SIZE=$(cat size.txt)
          echo "Browser bundle size: $SIZE bytes (limit $LIMIT)"
          if [ "$SIZE" -gt "$LIMIT" ]; then
            echo "::error::Browser bundle exceeds limit!"
            exit 1
          fi